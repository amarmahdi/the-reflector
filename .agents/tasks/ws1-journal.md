# Agent Task: Journal & Reflection System

## Context
You are working on **The Reflector**, an Expo/React Native discipline-tracking app. You are ONE of 8 agents working in parallel. You MUST only create/modify files listed below.

## Design System (MANDATORY)
- **Styling**: `styled-components/native` — NEVER use `StyleSheet.create`
- **Colors**: Import `{ COLORS }` from `@/constants/theme`
  - Backgrounds: `surface0` (#0D0D0D), `surface1` (#141414), `surface2` (#1C1C1C)
  - Accent: `crimson` (#1A6B3C — it's dark green despite the name)
  - Text: `textPrimary` (#F0EDE8), `textSecondary` (#8A8580), `textDim` (#4A4845)
  - Borders: `border` (#2A2A2A), `borderLight` (#333333)
- **Typography**: ALL headers and labels UPPERCASE. font-weight 900 for headings, 700 for labels. Letter-spacing 2-4px for headers.
- **Haptics**: Import `{ haptic }` from `@/lib/haptics` — use `haptic.light()` on button presses, `haptic.success()` on saves
- **Icons**: `@hugeicons/react-native` with `@hugeicons/core-free-icons`
- **Dark theme only**

## What Already Exists (DO NOT MODIFY)
- `types/models.ts` — Already has `JournalEntry`, `JournalMood`, `MOOD_CONFIG` types
- `store/useJournalStore.ts` — Already has full CRUD operations (`addJournalEntry`, `updateJournalEntry`, `deleteJournalEntry`, `searchJournal`, `getJournalByDate`, `getEntriesByMood`, `getEntriesByTag`, `getAllTags`)
- `store/useReflectorStore.ts` — DO NOT TOUCH
- `app/(tabs)/_layout.tsx` — DO NOT TOUCH
- `app/_layout.tsx` — DO NOT TOUCH

## Files to CREATE

### 1. `app/journal/index.tsx` — Journal Timeline Screen
Full-screen journal browser accessible via navigation (not a tab).

**Requirements:**
- Header: "THE JOURNAL" with accent styling matching other screens
- Search bar at top — filters entries in real-time by title, body, tags
- Mood filter row — 5 mood chips (using `MOOD_CONFIG` from models), tap to filter
- Tag filter — horizontal scrollable chips from `getAllTags()`
- Scrollable list of journal entries grouped by date (date headers like "APRIL 2, 2026")
- Each entry rendered as a `JournalCard` component
- FAB (floating action button) in bottom-right to add new entry — opens AddJournal modal
- Empty state when no entries: icon + "YOUR STORY BEGINS HERE" + subtitle
- Tapping a card navigates to `journal/[entryId]`

**Add Journal Modal (inline in this file):**
- Bottom sheet modal (same style as today.tsx add task modal)
- Title input
- Body multiline input (min 200px height)
- Mood picker row (5 emoji buttons)
- Tags input (comma-separated, auto-parse)
- CANCEL / SAVE buttons

### 2. `app/journal/[entryId].tsx` — Journal Entry Detail
Full view of a single journal entry.

**Requirements:**
- Show date, mood emoji + label, title, full body text
- Tags displayed as chips
- If `linkedGridId` exists, show "LINKED TO: [routine name] — DAY [X]" badge
- If `isAutoGenerated`, show "AUTO-GENERATED FROM REFLECTION" badge
- Edit button in header — toggles inline edit mode (fields become editable)
- Delete button — confirmation Alert, then delete and `router.back()`
- Back navigation

### 3. `components/JournalCard.tsx` — Reusable Entry Card
Used in the journal timeline list.

**Requirements:**
- Card with `surface1` background, `border` border, 8px border-radius
- Top row: mood emoji + date (formatted like "APR 2, 2026") + tag chips
- Title in `textPrimary`, 14px, weight 700
- Body preview (2 lines max, truncated with ellipsis)
- If auto-generated: small "REFLECTION" badge in crimson
- `onPress` prop for navigation

### 4. `components/MoodPicker.tsx` — Mood Selection Row
Reusable mood selector.

**Requirements:**
- Row of 5 pressable mood options
- Each shows emoji (from `MOOD_CONFIG`) + label below
- Selected mood has `crimson` border and `crimsonGlow` background
- `onSelect` callback prop
- `selectedMood` prop
- Haptic feedback on selection

### 5. MODIFY `app/fire.tsx` — Auto-Journal from Reflections
After the user submits a reflection in the fire screen, auto-create a journal entry.

**Changes:**
- Import `useJournalStore` from `@/store/useJournalStore`
- In `handleSubmit()`, after `markDayScarred(...)`, add:
```typescript
const journalStore = useJournalStore.getState();
journalStore.addJournalEntry({
  date: todayStart,
  title: `Missed: ${routine?.title ?? 'Unknown'} — Day ${current.day.dayIndex}`,
  body: reason.trim(),
  mood: 'rough',
  tags: ['reflection', 'missed-day'],
  linkedGridId: current.grid.id,
  linkedDayIndex: current.day.dayIndex,
  isAutoGenerated: true,
});
```

## Navigation
The journal screens are stack screens, not tabs. They should be navigated to via `router.push('/journal')` from other screens. Other agents will add navigation links — your job is just to make the screens work if someone navigates to `/journal` or `/journal/[entryId]`.

## Verification
1. Run `npx expo start` — no TypeScript errors
2. Navigate to `/journal` — should render empty state
3. Tap FAB → fill form → save → entry appears in list
4. Tap entry → detail screen with edit/delete
5. The fire.tsx reflection flow should auto-create journal entries
